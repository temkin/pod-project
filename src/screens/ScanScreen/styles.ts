const styles = {
  instructionsContainer: {
    position: "absolute",
    zIndex: 2,
    bottom: 0,
    width: "100%",
    display: "flex",
    justifyContent: "center",
  },
};

export default styles;
